{
  "name": "calendar",
  "version": "1.0.0",
  "default": "index.html",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html  --dist-dir ./dist"
  },
  "author": "Abid Khan",
  "license": "ISC",
  "description": "A simple calendar application built with HTML, CSS, and JavaScript to display the calendar for any given year.",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
